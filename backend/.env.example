# Copy this file to .env and adjust values for your local environment.
# Never commit the real .env file.

# Server
PORT=5000
NODE_ENV=development

# MongoDB
# Local Docker Compose mongo service uses the hostname "mongo"
MONGO_URI=mongodb+srv://megaurav402815_db_user:me_gaurav_7@cluster0.4imffda.mongodb.net/cloudsentinel?appName=Cluster0

# JWT
# Replace with a long random string in real use. This is a placeholder only.
JWT_SECRET=replace_this_with_a_long_random_development_secret
JWT_EXPIRES_IN=8h

# Synthetic log generator
LOG_GENERATOR_INTERVAL_MS=2000
LOG_GENERATOR_ENABLED=true

# CORS
CORS_ORIGIN=http://localhost:5173
