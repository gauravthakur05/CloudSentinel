# Base URL of the backend API. When running via Docker Compose, the
# frontend container talks to the backend over the browser, so this must
# be a URL the *browser* can reach - localhost:5000 (the published port),
# not the internal Docker service name "backend".
VITE_API_BASE_URL=http://localhost:5000
